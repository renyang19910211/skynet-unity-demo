-- The DAO object for data in collection Account.
-- This object should not have states, so use upvalues instead of use members

local db_account = {
    _id = "",
    account = nil,
    password = nil,
}

return db_account
